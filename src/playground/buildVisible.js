console.log("app is running");

const element = (
    <div>
        <p>render this to the window</p>
    </div>
);

ReactDOM.render(element, appRoot);

const appRoot = document.getElementById('app');










